# Module 13 Interview Questions — CSS Architecture & Real-World Project

1. What problem does BEM naming solve in a large, multi-contributor codebase?
2. What's the difference between a BEM Element and a BEM Modifier?
3. What core principle does the ITCSS "inverted triangle" describe?
4. Why is it risky for component CSS files to `@import` one another?
5. Name the three stages of the browser's render pipeline, and which is most expensive.
6. Is deep selector nesting a meaningful performance concern in modern browsers? Why or why not?
7. What does `content-visibility: auto` do, and what property is it typically paired with?
8. What's the fundamental difference between how a Sass variable and a native CSS custom property are resolved?
9. Which Sass feature still has no native CSS equivalent, and why does that matter for real-world codebases?
10. How would you decide whether a new project should use Sass or plain modern CSS?
11. Walk through how you'd architect the CSS for a brand-new, mid-size web application from scratch.
12. What accessibility considerations would you check for in a final review pass of a real project?
