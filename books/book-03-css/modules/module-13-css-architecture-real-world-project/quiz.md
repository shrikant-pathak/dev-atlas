# Module 13 Quiz — CSS Architecture & Real-World Project

1. In BEM, which punctuation connects a Block to an Element?
   a) A single dash
   b) Two underscores
   c) Two dashes
   d) A colon

2. In BEM, which punctuation connects a Block/Element to a Modifier?
   a) Two underscores
   b) Two dashes
   c) A single underscore
   d) A period

3. What does ITCSS's "inverted triangle" primarily describe?
   a) File naming conventions
   b) Ordering CSS from broad/low-specificity to narrow/high-specificity
   c) A grid layout technique
   d) A Sass-only feature

4. Which render pipeline stage is generally the most expensive?
   a) Composite
   b) Paint
   c) Layout
   d) They are all equal

5. Is deeply nested selector complexity a major runtime performance concern in modern browsers?
   a) Yes, always avoid nesting for performance
   b) No — it's mainly a maintainability concern, not a runtime performance one
   c) Only on mobile devices
   d) Only in Sass-compiled CSS

6. What does `content-visibility: auto` do?
   a) Hides content permanently
   b) Skips layout/paint work for off-screen elements until they're needed
   c) Compresses CSS files
   d) Enables dark mode automatically

7. What is a Sass mixin, and does native CSS have a direct equivalent?
   a) A variable; yes, custom properties replace it fully
   b) A reusable, parameterized block of declarations; no direct native equivalent
   c) A media query shortcut; yes, nesting replaces it
   d) A color function; yes, `color-mix()` replaces it

8. What is the capstone project in this module?
   a) Building a new HTML site from scratch
   b) Styling the Book 02 HTML portfolio using techniques from all 13 modules
   c) Writing a Sass compiler
   d) Building a component library with no HTML

<details><summary>Answer Key</summary>

1. b
2. b
3. b
4. c
5. b
6. b
7. b
8. b

</details>
